import { infoBar } from "./info.css";

const Info = () => {
  return (
    <div className={infoBar}>
      Free shipping on all orders over $200! For a limited time only.
    </div>
  );
};

export default Info;
